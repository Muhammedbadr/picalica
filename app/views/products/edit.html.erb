<%# app/views/products/edit.html.erb %>
<% content_for :title, "Editing Product - Step #{@step}" %>

  
  <div class="max-w-3xl mx-auto text-center mb-2 pt-12">
    <h1 class="text-4xl font-bold tracking-tight" style="color: #5D5899;">
      Editing: <%= @step == "1" ? "General Information" : "Product Assets" %>
    </h1>
    <p class="text-gray-500 mt-2 text-sm">Update your product details in two simple steps</p>
  </div>
    <% if @step == "1" %>
      <%# This renders your new.html.erb code %>
      <%= render template: "products/new" %>
    <% else %>
      <%# This renders your step_two.html.erb code %>
      <%= render template: "products/step_two" %>
    <% end %>

  <div class="mt-8 flex gap-4">
    <%= link_to "Cancel", products_path, class: "text-gray-400 font-bold uppercase text-xs" %>
  </div>











